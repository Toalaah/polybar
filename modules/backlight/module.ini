type=internal/backlight

card=intel_backlight
enable-scroll=false

format=<bar>
format-background=${colors.none}
format-prefix="  "
format-prefix-foreground=${colors.color5}

bar-width=10
bar-indicator=─
bar-fill=─
bar-empty=─

bar-empty-foreground=${colors.fg}
bar-fill-foreground=${colors.color5}
bar-indicator-foreground=${colors.color5}
